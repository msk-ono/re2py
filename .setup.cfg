[metadata]
name = re2py
version = 0.0.1

[options]
package_dir =
    =src
packages = find:
install_requires =
    graphviz

[options.extras_require]
develop =
    pytest
    black

[options.packages.find]
where=src
